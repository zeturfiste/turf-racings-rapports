name: Re-scrape Missing Courses

on:
  workflow_dispatch:
    inputs:
      max_courses:
        description: 'Nombre max de courses à récupérer (vide = toutes)'
        required: false
        type: string
      batch_size:
        description: 'Taille initiale des batchs'
        required: false
        default: '200'
        type: string

jobs:
  rescrape-courses:
    runs-on: ubuntu-latest
    timeout-minutes: 180
    
    steps:
      - name: "📊 Espace disque initial"
        run: |
          echo "=========================================="
          echo "ESPACE DISQUE INITIAL"
          echo "=========================================="
          df -h | grep -E "Filesystem|/$"
      
      - name: "🧹 Nettoyage cache runner"
        run: |
          echo "=========================================="
          echo "NETTOYAGE CACHE RUNNER"
          echo "=========================================="
          
          echo "=== Avant nettoyage ==="
          if [ -d "/home/runner/actions-runner/cached" ]; then
            du -sh /home/runner/actions-runner/cached/ 2>/dev/null || echo "N/A"
          fi
          df -h | grep -E "/$"
          
          echo ""
          echo "🧹 Nettoyage du cache..."
          sudo rm -rf /home/runner/actions-runner/cached/* 2>/dev/null || true
          
          echo ""
          echo "=== Après nettoyage ==="
          if [ -d "/home/runner/actions-runner/cached" ]; then
            du -sh /home/runner/actions-runner/cached/ 2>/dev/null || echo "Vide"
          fi
          df -h | grep -E "/$"
      
      - name: "🧹 Nettoyage agressif du runner"
        run: |
          echo "=========================================="
          echo "NETTOYAGE AGRESSIF"
          echo "=========================================="
          
          echo "🧹 Docker..."
          docker system prune -af --volumes 2>/dev/null || true
          
          echo "🧹 APT cache..."
          sudo apt-get clean 2>/dev/null || true
          sudo rm -rf /var/lib/apt/lists/* 2>/dev/null || true
          
          echo "🧹 pip cache..."
          pip cache purge 2>/dev/null || true
          
          echo "🧹 npm/yarn..."
          sudo rm -rf /usr/local/lib/node_modules 2>/dev/null || true
          sudo rm -rf ~/.npm ~/.yarn 2>/dev/null || true
          
          echo "🧹 Android SDK (~8GB)..."
          sudo rm -rf /usr/local/lib/android 2>/dev/null || true
          sudo rm -rf $ANDROID_HOME 2>/dev/null || true
          
          echo "🧹 .NET (~3GB)..."
          sudo rm -rf /usr/share/dotnet 2>/dev/null || true
          
          echo "🧹 Fichiers temporaires..."
          sudo rm -rf /tmp/* /var/tmp/* 2>/dev/null || true
          
          echo "🧹 Logs système..."
          sudo journalctl --vacuum-time=1s 2>/dev/null || true
          sudo rm -rf /var/log/* 2>/dev/null || true
          
          echo "✅ Nettoyage terminé"
      
      - name: "💾 Espace après nettoyage"
        run: |
          echo "=========================================="
          echo "ESPACE APRÈS NETTOYAGE"
          echo "=========================================="
          df -h
          FREE_SPACE=$(df -h / | awk 'NR==2 {print $4}')
          echo ""
          echo "💾 Espace libre: $FREE_SPACE"
      
      - name: "📥 Checkout (minimal + sparse)"
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
          sparse-checkout: |
            verify.py
            rescrape_courses.py
            verification_report.txt
      
      - name: "💾 Espace après checkout"
        run: |
          echo "=== Espace après checkout ==="
          df -h | grep -E "Filesystem|/$"
          echo ""
          echo "=== Taille .git ==="
          du -sh .git 2>/dev/null || echo "N/A"
      
      - name: "🌳 Enable sparse checkout pour les données"
        run: |
          git sparse-checkout add resultats-et-rapports
          
          echo "🧹 Nettoyage cache Git..."
          git gc --aggressive --prune=now
          git prune
      
      - name: "💾 Espace après sparse checkout"
        run: |
          echo "=== Espace après sparse checkout ==="
          df -h | grep -E "Filesystem|/$"
          if [ -d "resultats-et-rapports" ]; then
            echo ""
            echo "=== Taille données ==="
            du -sh resultats-et-rapports/ 2>/dev/null || echo "N/A"
          fi
      
      - name: "🐍 Setup Python"
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: "📦 Install dependencies (no cache)"
        run: |
          pip install --no-cache-dir aiohttp beautifulsoup4 lxml
          pip cache purge 2>/dev/null || true
      
      - name: "📋 Vérifier le rapport"
        id: check_report
        run: |
          if [ ! -f "verification_report.txt" ]; then
            echo "⚠️  Rapport absent, exécution de verify.py..."
            python verify.py || true
          fi
          
          if [ -f "verification_report.txt" ]; then
            echo "=== Courses manquantes ==="
            MISSING_COURSES=$(grep -c "❌.*\.html" verification_report.txt || echo "0")
            echo "Total: $MISSING_COURSES courses"
            echo "missing_count=$MISSING_COURSES" >> $GITHUB_OUTPUT
            
            echo ""
            echo "=== Aperçu (10 premières dates) ==="
            grep "DATE:.*INCOMPLETE" verification_report.txt | head -n 10
          else
            echo "missing_count=0" >> $GITHUB_OUTPUT
          fi
      
      - name: "💾 Espace avant scraping"
        run: |
          echo "=== Espace avant scraping ==="
          df -h | grep -E "Filesystem|/$"
          FREE_GB=$(df -BG / | awk 'NR==2 {print $4}' | sed 's/G//')
          echo ""
          echo "💾 Espace disponible: ${FREE_GB} GB"
          
          if [ "$FREE_GB" -lt "5" ]; then
            echo "⚠️  WARNING: Espace faible!"
          fi
      
      - name: "🚀 Re-scraping des courses"
        if: steps.check_report.outputs.missing_count != '0'
        env:
          MAX_COURSES: ${{ github.event.inputs.max_courses || '' }}
          BATCH_SIZE: ${{ github.event.inputs.batch_size || '200' }}
        run: |
          # Monitor disk space en arrière-plan
          (while true; do
            FREE_GB=$(df -BG / | awk 'NR==2 {print $4}' | sed 's/G//')
            if [ "$FREE_GB" -lt "2" ]; then
              echo "⚠️  [MONITOR] Espace critique: ${FREE_GB}GB"
            fi
            sleep 30
          done) &
          MONITOR_PID=$!
          
          # Run scraping
          if [ -z "$MAX_COURSES" ]; then
            echo "🚀 Re-scraping TOUTES les courses manquantes"
            python rescrape_courses.py --batch-size $BATCH_SIZE
          else
            echo "🚀 Re-scraping max $MAX_COURSES courses"
            python rescrape_courses.py --max-courses $MAX_COURSES --batch-size $BATCH_SIZE
          fi
          
          # Stop monitor
          kill $MONITOR_PID 2>/dev/null || true
      
      - name: "📊 Aucune course manquante"
        if: steps.check_report.outputs.missing_count == '0'
        run: |
          echo "✅ Aucune course manquante détectée!"
          echo "Tous les fichiers sont déjà présents."
      
      - name: "💾 Espace après scraping"
        if: always()
        run: |
          echo "=== Espace après scraping ==="
          df -h | grep -E "Filesystem|/$"
          if [ -d "resultats-et-rapports" ]; then
            echo ""
            echo "=== Taille des données ==="
            du -sh resultats-et-rapports/ 2>/dev/null || echo "N/A"
          fi
      
      - name: "🆘 Nettoyage d'urgence si espace critique"
        if: always()
        run: |
          FREE_GB=$(df -BG / | awk 'NR==2 {print $4}' | sed 's/G//')
          echo "Espace actuel: ${FREE_GB}GB"
          
          if [ "$FREE_GB" -lt "3" ]; then
            echo "⚠️  Espace critique! Nettoyage d'urgence..."
            
            find . -type d -name __pycache__ -exec rm -rf {} + 2>/dev/null || true
            find . -type f -name "*.pyc" -delete 2>/dev/null || true
            
            git reflog expire --expire=now --all
            git gc --aggressive --prune=now
            
            echo "=== Nouvel espace ==="
            df -h | grep -E "Filesystem|/$"
          fi
      
      - name: "✅ Vérification post-scraping"
        run: |
          echo "=== Nouvelle vérification ==="
          python verify.py || true
      
      - name: "🧹 Nettoyage avant commit"
        run: |
          echo "🧹 Nettoyage avant commit..."
          git gc --aggressive --prune=now
          df -h | grep -E "Filesystem|/$"
      
      - name: "📤 Commit verification report"
        run: |
          git config user.name "GitHub Actions Bot"
          git config user.email "actions@github.com"
          git add verification_report.txt
          if ! git diff --staged --quiet; then
            git commit -m "Update verification report - $(date +'%Y-%m-%d %H:%M')"
            git push
          fi
      
      - name: "📊 Statut final"
        if: always()
        run: |
          echo "=========================================="
          echo "STATUT FINAL"
          echo "=========================================="
          
          df -h | grep -E "Filesystem|/$"
          echo ""
          
          if [ -f "verification_report.txt" ]; then
            echo "=== Résultat de la vérification ==="
            grep -E "Total de dates|Complètes|Incomplètes" verification_report.txt | head -n 4
            echo ""
            
            STILL_MISSING=$(grep -c "❌.*\.html" verification_report.txt || echo "0")
            if [ "$STILL_MISSING" -gt "0" ]; then
              echo "⚠️  Il reste $STILL_MISSING courses manquantes"
              echo "💡 Relancez le workflow pour continuer"
            else
              echo "🎉 Toutes les courses sont complètes!"
            fi
          fi
      
      - name: "📄 Upload rapport final"
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: verification-report-${{ github.run_number }}
          path: verification_report.txt
          retention-days: 7
