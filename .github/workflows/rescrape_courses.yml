name: Re-scrape Missing Courses (par années)

on:
  workflow_dispatch:
    inputs:
      jobs_count:
        description: "Nombre de jobs parallèles (1 à 3)"
        required: true
        default: "3"
        type: string

      years_job_1:
        description: "Années pour le job 1 (ex: '2008 2011 2014')"
        required: false
        default: "2008 2011 2014 2017 2020 2023"
        type: string

      years_job_2:
        description: "Années pour le job 2 (ex: '2009 2012 2015')"
        required: false
        default: "2009 2012 2015 2018 2021 2024"
        type: string

      years_job_3:
        description: "Années pour le job 3 (ex: '2010 2013 2016 2019 2022 2025')"
        required: false
        default: "2010 2013 2016 2019 2022 2025"
        type: string

      max_courses:
        description: "Limite globale de courses pour ce run (optionnel, pour tests)"
        required: false
        type: string

permissions:
  contents: write   # pour permettre les commits/push depuis rescrape_courses.py

jobs:
  # ----------------------------------------------------------
  # Job 1
  # ----------------------------------------------------------
  rescrape-job-1:
    name: "Job 1 (années: ${{ github.event.inputs.years_job_1 }})"
    if: ${{ github.event.inputs.jobs_count == '1' || github.event.inputs.jobs_count == '2' || github.event.inputs.jobs_count == '3' }}
    runs-on: ubuntu-latest

    env:
      YEARS: ${{ github.event.inputs.years_job_1 }}
      MAX_COURSES: ${{ github.event.inputs.max_courses }}
      TARGET_BRANCH: ${{ github.ref_name }}

    steps:
      - name: "📊 Espace disque initial"
        run: |
          echo "=== ESPACE DISQUE INITIAL (Job 1) ==="
          df -h
          echo
          echo "Top 20 plus gros dossiers (global) :"
          sudo du -xhd1 / | sort -h | tail -n 20 || true

      - name: "🧹 Nettoyage agressif AVANT clone"
        run: |
          echo "=== Nettoyage agressif AVANT clone (Job 1) ==="
          df -h

          echo
          echo "🧹 Suppression gros SDK/runtimes inutiles..."
          sudo rm -rf /usr/local/lib/android || true
          sudo rm -rf "$ANDROID_HOME" || true
          sudo rm -rf /usr/local/.ghcup || true
          sudo rm -rf /opt/ghc || true
          sudo rm -rf /usr/share/dotnet || true
          sudo rm -rf /usr/share/swift || true
          sudo rm -rf /opt/hostedtoolcache || true

          echo "🧹 Caches et temporaires..."
          sudo rm -rf /var/lib/apt/lists/* /var/cache/apt/* || true
          sudo rm -rf /tmp/* /var/tmp/* || true
          rm -rf ~/.cache ~/.npm ~/.yarn || true

          echo
          echo "=== ESPACE APRÈS CLEANUP AVANT CLONE (Job 1) ==="
          df -h

      - name: "📥 Sparse clone du repo (Job 1)"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -e
          echo "Années (YEARS) pour ce job : '$YEARS'"

          REPO_URL="https://github.com/${{ github.repository }}.git"
          BRANCH="${TARGET_BRANCH}"

          echo "Clonage partiel (filter=blob:none, --sparse) sur branche $BRANCH"
          git clone --filter=blob:none --no-checkout --depth 1 --sparse \
            "https://${GITHUB_TOKEN}@github.com/${{ github.repository }}.git" .

          echo
          echo "Configuration sparse-checkout pour les fichiers nécessaires..."
          # Construire la liste des patterns (fichiers + années)
          PATTERNS="rescrape_courses.py verification_report.txt"
          for y in $YEARS; do
            PATTERNS="$PATTERNS resultats-et-rapports/$y"
          done

          echo "Patterns : $PATTERNS"
          git sparse-checkout init --no-cone
          git sparse-checkout set $PATTERNS

          echo
          echo "Checkout de la branche $BRANCH"
          git checkout "$BRANCH"

          echo
          echo "=== ESPACE APRÈS SPARSE CLONE (Job 1) ==="
          df -h
          echo
          echo "Taille du repo (Job 1) :"
          du -sh . || true
          if [ -d "resultats-et-rapports" ]; then
            du -sh resultats-et-rapports/* || true
          fi

      - name: "🐍 Setup Python"
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: "📦 Install dependencies (aiohttp uniquement)"
        run: |
          pip install --no-cache-dir aiohttp
          pip cache purge 2>/dev/null || true

      - name: "💾 Espace avant scraping (Job 1)"
        run: |
          echo "=== ESPACE AVANT SCRAPING (Job 1) ==="
          df -h
          if [ -d "resultats-et-rapports" ]; then
            du -sh resultats-et-rapports || true
          fi

      - name: "🚀 Re-scrape courses (Job 1)"
        run: |
          set -e

          echo "Années (input brut) pour ce job: '${YEARS}'"
          YEARS_TRIMMED=$(echo "$YEARS" | xargs || true)
          YEARS_CSV=$(echo "$YEARS_TRIMMED" | tr ' ' ',' | tr -s ',' | sed 's/^,//;s/,$//' || true)

          ARGS=()

          if [ -n "$YEARS_CSV" ]; then
            echo "→ Années envoyées à rescrape_courses.py : $YEARS_CSV"
            ARGS+=(--years "$YEARS_CSV")
          else
            echo "⚠️  Aucune année spécifiée pour ce job, rescrape_courses.py traitera toutes les années présentes dans verification_report.txt."
          fi

          if [ -n "$MAX_COURSES" ]; then
            echo "→ Limite globale de courses: $MAX_COURSES"
            ARGS+=(--max-courses "$MAX_COURSES")
          fi

          echo "Commande exécutée : python rescrape_courses.py ${ARGS[*]}"
          python rescrape_courses.py "${ARGS[@]}"

      - name: "💾 Espace après scraping (Job 1)"
        if: always()
        run: |
          echo "=== ESPACE APRÈS SCRAPING (Job 1) ==="
          df -h
          if [ -d "resultats-et-rapports" ]; then
            du -sh resultats-et-rapports || true
          fi

  # ----------------------------------------------------------
  # Job 2
  # ----------------------------------------------------------
  rescrape-job-2:
    name: "Job 2 (années: ${{ github.event.inputs.years_job_2 }})"
    if: ${{ github.event.inputs.jobs_count == '2' || github.event.inputs.jobs_count == '3' }}
    runs-on: ubuntu-latest

    env:
      YEARS: ${{ github.event.inputs.years_job_2 }}
      MAX_COURSES: ${{ github.event.inputs.max_courses }}
      TARGET_BRANCH: ${{ github.ref_name }}

    steps:
      - name: "📊 Espace disque initial"
        run: |
          echo "=== ESPACE DISQUE INITIAL (Job 2) ==="
          df -h
          echo
          echo "Top 20 plus gros dossiers (global) :"
          sudo du -xhd1 / | sort -h | tail -n 20 || true

      - name: "🧹 Nettoyage agressif AVANT clone"
        run: |
          echo "=== Nettoyage agressif AVANT clone (Job 2) ==="
          df -h

          echo
          echo "🧹 Suppression gros SDK/runtimes inutiles..."
          sudo rm -rf /usr/local/lib/android || true
          sudo rm -rf "$ANDROID_HOME" || true
          sudo rm -rf /usr/local/.ghcup || true
          sudo rm -rf /opt/ghc || true
          sudo rm -rf /usr/share/dotnet || true
          sudo rm -rf /usr/share/swift || true
          sudo rm -rf /opt/hostedtoolcache || true

          echo "🧹 Caches et temporaires..."
          sudo rm -rf /var/lib/apt/lists/* /var/cache/apt/* || true
          sudo rm -rf /tmp/* /var/tmp/* || true
          rm -rf ~/.cache ~/.npm ~/.yarn || true

          echo
          echo "=== ESPACE APRÈS CLEANUP AVANT CLONE (Job 2) ==="
          df -h

      - name: "📥 Sparse clone du repo (Job 2)"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -e
          echo "Années (YEARS) pour ce job : '$YEARS'"

          REPO_URL="https://github.com/${{ github.repository }}.git"
          BRANCH="${TARGET_BRANCH}"

          echo "Clonage partiel (filter=blob:none, --sparse) sur branche $BRANCH"
          git clone --filter=blob:none --no-checkout --depth 1 --sparse \
            "https://${GITHUB_TOKEN}@github.com/${{ github.repository }}.git" .

          echo
          echo "Configuration sparse-checkout pour les fichiers nécessaires..."
          PATTERNS="rescrape_courses.py verification_report.txt"
          for y in $YEARS; do
            PATTERNS="$PATTERNS resultats-et-rapports/$y"
          done

          echo "Patterns : $PATTERNS"
          git sparse-checkout init --no-cone
          git sparse-checkout set $PATTERNS

          echo
          echo "Checkout de la branche $BRANCH"
          git checkout "$BRANCH"

          echo
          echo "=== ESPACE APRÈS SPARSE CLONE (Job 2) ==="
          df -h
          echo
          echo "Taille du repo (Job 2) :"
          du -sh . || true
          if [ -d "resultats-et-rapports" ]; then
            du -sh resultats-et-rapports/* || true
          fi

      - name: "🐍 Setup Python"
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: "📦 Install dependencies (aiohttp uniquement)"
        run: |
          pip install --no-cache-dir aiohttp
          pip cache purge 2>/dev/null || true

      - name: "💾 Espace avant scraping (Job 2)"
        run: |
          echo "=== ESPACE AVANT SCRAPING (Job 2) ==="
          df -h
          if [ -d "resultats-et-rapports" ]; then
            du -sh resultats-et-rapports || true
          fi

      - name: "🚀 Re-scrape courses (Job 2)"
        run: |
          set -e

          echo "Années (input brut) pour ce job: '${YEARS}'"
          YEARS_TRIMMED=$(echo "$YEARS" | xargs || true)
          YEARS_CSV=$(echo "$YEARS_TRIMMED" | tr ' ' ',' | tr -s ',' | sed 's/^,//;s/,$//' || true)

          ARGS=()

          if [ -n "$YEARS_CSV" ]; then
            echo "→ Années envoyées à rescrape_courses.py : $YEARS_CSV"
            ARGS+=(--years "$YEARS_CSV")
          else
            echo "⚠️  Aucune année spécifiée pour ce job, rescrape_courses.py traitera toutes les années présentes dans verification_report.txt."
          fi

          if [ -n "$MAX_COURSES" ]; then
            echo "→ Limite globale de courses: $MAX_COURSES"
            ARGS+=(--max-courses "$MAX_COURSES")
          fi

          echo "Commande exécutée : python rescrape_courses.py ${ARGS[*]}"
          python rescrape_courses.py "${ARGS[@]}"

      - name: "💾 Espace après scraping (Job 2)"
        if: always()
        run: |
          echo "=== ESPACE APRÈS SCRAPING (Job 2) ==="
          df -h
          if [ -d "resultats-et-rapports" ]; then
            du -sh resultats-et-rapports || true
          fi

  # ----------------------------------------------------------
  # Job 3
  # ----------------------------------------------------------
  rescrape-job-3:
    name: "Job 3 (années: ${{ github.event.inputs.years_job_3 }})"
    if: ${{ github.event.inputs.jobs_count == '3' }}
    runs-on: ubuntu-latest

    env:
      YEARS: ${{ github.event.inputs.years_job_3 }}
      MAX_COURSES: ${{ github.event.inputs.max_courses }}
      TARGET_BRANCH: ${{ github.ref_name }}

    steps:
      - name: "📊 Espace disque initial"
        run: |
          echo "=== ESPACE DISQUE INITIAL (Job 3) ==="
          df -h
          echo
          echo "Top 20 plus gros dossiers (global) :"
          sudo du -xhd1 / | sort -h | tail -n 20 || true

      - name: "🧹 Nettoyage agressif AVANT clone"
        run: |
          echo "=== Nettoyage agressif AVANT clone (Job 3) ==="
          df -h

          echo
          echo "🧹 Suppression gros SDK/runtimes inutiles..."
          sudo rm -rf /usr/local/lib/android || true
          sudo rm -rf "$ANDROID_HOME" || true
          sudo rm -rf /usr/local/.ghcup || true
          sudo rm -rf /opt/ghc || true
          sudo rm -rf /usr/share/dotnet || true
          sudo rm -rf /usr/share/swift || true
          sudo rm -rf /opt/hostedtoolcache || true

          echo "🧹 Caches et temporaires..."
          sudo rm -rf /var/lib/apt/lists/* /var/cache/apt/* || true
          sudo rm -rf /tmp/* /var/tmp/* || true
          rm -rf ~/.cache ~/.npm ~/.yarn || true

          echo
          echo "=== ESPACE APRÈS CLEANUP AVANT CLONE (Job 3) ==="
          df -h

      - name: "📥 Sparse clone du repo (Job 3)"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -e
          echo "Années (YEARS) pour ce job : '$YEARS'"

          REPO_URL="https://github.com/${{ github.repository }}.git"
          BRANCH="${TARGET_BRANCH}"

          echo "Clonage partiel (filter=blob:none, --sparse) sur branche $BRANCH"
          git clone --filter=blob:none --no-checkout --depth 1 --sparse \
            "https://${GITHUB_TOKEN}@github.com/${{ github.repository }}.git" .

          echo
          echo "Configuration sparse-checkout pour les fichiers nécessaires..."
          PATTERNS="rescrape_courses.py verification_report.txt"
          for y in $YEARS; do
            PATTERNS="$PATTERNS resultats-et-rapports/$y"
          done

          echo "Patterns : $PATTERNS"
          git sparse-checkout init --no-cone
          git sparse-checkout set $PATTERNS

          echo
          echo "Checkout de la branche $BRANCH"
          git checkout "$BRANCH"

          echo
          echo "=== ESPACE APRÈS SPARSE CLONE (Job 3) ==="
          df -h
          echo
          echo "Taille du repo (Job 3) :"
          du -sh . || true
          if [ -d "resultats-et-rapports" ]; then
            du -sh resultats-et-rapports/* || true
          fi

      - name: "🐍 Setup Python"
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: "📦 Install dependencies (aiohttp uniquement)"
        run: |
          pip install --no-cache-dir aiohttp
          pip cache purge 2>/dev/null || true

      - name: "💾 Espace avant scraping (Job 3)"
        run: |
          echo "=== ESPACE AVANT SCRAPING (Job 3) ==="
          df -h
          if [ -d "resultats-et-rapports" ]; then
            du -sh resultats-et-rapports || true
          fi

      - name: "🚀 Re-scrape courses (Job 3)"
        run: |
          set -e

          echo "Années (input brut) pour ce job: '${YEARS}'"
          YEARS_TRIMMED=$(echo "$YEARS" | xargs || true)
          YEARS_CSV=$(echo "$YEARS_TRIMMED" | tr ' ' ',' | tr -s ',' | sed 's/^,//;s/,$//' || true)

          ARGS=()

          if [ -n "$YEARS_CSV" ]; then
            echo "→ Années envoyées à rescrape_courses.py : $YEARS_CSV"
            ARGS+=(--years "$YEARS_CSV")
          else
            echo "⚠️  Aucune année spécifiée pour ce job, rescrape_courses.py traitera toutes les années présentes dans verification_report.txt."
          fi

          if [ -n "$MAX_COURSES" ]; then
            echo "→ Limite globale de courses: $MAX_COURSES"
            ARGS+=(--max-courses "$MAX_COURSES")
          fi

          echo "Commande exécutée : python rescrape_courses.py ${ARGS[*]}"
          python rescrape_courses.py "${ARGS[@]}"

      - name: "💾 Espace après scraping (Job 3)"
        if: always()
        run: |
          echo "=== ESPACE APRÈS SCRAPING (Job 3) ==="
          df -h
          if [ -d "resultats-et-rapports" ]; then
            du -sh resultats-et-rapports || true
          fi
