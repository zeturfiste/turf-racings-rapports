name: Re-scrape Missing Courses

on:
  workflow_dispatch:

jobs:
  rescrape-courses:
    runs-on: ubuntu-latest
    timeout-minutes: 180

    steps:
      - name: "📊 Espace disque initial"
        run: |
          echo "=========================================="
          echo "ESPACE DISQUE INITIAL"
          echo "=========================================="
          df -h | grep -E "Filesystem|/$"

      - name: "🧹 Nettoyage agressif du runner"
        run: |
          echo "=========================================="
          echo "NETTOYAGE AGRESSIF DU RUNNER"
          echo "=========================================="

          echo "=== Espace avant nettoyage ==="
          df -h | grep -E "Filesystem|/$"

          echo ""
          echo "🧹 Docker..."
          docker system prune -af --volumes 2>/dev/null || true

          echo "🧹 APT cache..."
          sudo apt-get clean 2>/dev/null || true
          sudo rm -rf /var/lib/apt/lists/* 2>/dev/null || true

          echo "🧹 pip cache..."
          pip cache purge 2>/dev/null || true
          rm -rf ~/.cache/pip 2>/dev/null || true

          echo "🧹 npm/yarn globaux..."
          sudo rm -rf /usr/local/lib/node_modules 2>/dev/null || true
          sudo rm -rf ~/.npm ~/.yarn 2>/dev/null || true

          echo "🧹 Android SDK (~8GB)..."
          sudo rm -rf /usr/local/lib/android 2>/dev/null || true
          sudo rm -rf "$ANDROID_HOME" 2>/dev/null || true

          echo "🧹 .NET SDK (~3GB)..."
          sudo rm -rf /usr/share/dotnet 2>/dev/null || true

          echo "🧹 CodeQL..."
          sudo rm -rf /opt/hostedtoolcache/CodeQL 2>/dev/null || true

          echo "🧹 Haskell (ghcup)..."
          sudo rm -rf /usr/local/.ghcup 2>/dev/null || true

          echo "🧹 Swift toolchain..."
          sudo rm -rf /usr/share/swift 2>/dev/null || true

          echo "🧹 Fichiers temporaires..."
          sudo rm -rf /tmp/* /var/tmp/* 2>/dev/null || true

          echo "🧹 Logs système..."
          sudo journalctl --vacuum-time=1s 2>/dev/null || true
          sudo rm -rf /var/log/* 2>/dev/null || true

          echo ""
          echo "=== Espace après nettoyage ==="
          df -h | grep -E "Filesystem|/$"

      - name: "📥 Checkout (sparse)"
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
          sparse-checkout: |
            verify.py
            rescrape_courses.py
            verification_report.txt
            resultats-et-rapports

      - name: "💾 Espace après checkout"
        run: |
          echo "=========================================="
          echo "ESPACE APRÈS CHECKOUT"
          echo "=========================================="
          df -h | grep -E "Filesystem|/$"
          echo ""
          echo "=== Taille du repo ==="
          du -sh . 2>/dev/null || echo "N/A"
          echo ""
          echo "=== Taille .git ==="
          du -sh .git 2>/dev/null || echo "N/A"

      - name: "🐍 Setup Python"
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: "📦 Install dependencies"
        run: |
          pip install --no-cache-dir aiohttp beautifulsoup4 lxml
          pip cache purge 2>/dev/null || true

      - name: "📋 Préparer le rapport de vérification"
        run: |
          if [ ! -f "verification_report.txt" ]; then
            echo "⚠️  verification_report.txt absent, exécution de verify.py..."
            python verify.py || true
          fi

          if [ ! -f "verification_report.txt" ]; then
            echo "❌ Impossible de générer verification_report.txt, arrêt."
            exit 1
          fi

          echo "✅ verification_report.txt prêt."

      - name: "🚀 Re-scraping des courses manquantes"
        run: |
          echo "=========================================="
          echo "LANCEMENT DU RE-SCRAPING (concurrency 100)"
          echo "=========================================="
          python rescrape_courses.py --concurrency 100

      - name: "✅ Nouvelle vérification post-scraping"
        run: |
          echo "=========================================="
          echo "VÉRIFICATION POST-SCRAPING"
          echo "=========================================="
          python verify.py || true

      - name: "📤 Commit verification report"
        run: |
          git config user.name "GitHub Actions Bot"
          git config user.email "actions@github.com"
          git add verification_report.txt
          if ! git diff --staged --quiet; then
            git commit -m "Update verification report - $(date +'%Y-%m-%d %H:%M')"
            git push
          else:
            echo "ℹ️  Aucun changement à committer pour verification_report.txt"
          fi

      - name: "📄 Upload rapport final"
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: verification-report-${{ github.run_number }}
          path: verification_report.txt
          retention-days: 7
